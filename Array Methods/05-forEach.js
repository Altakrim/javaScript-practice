// 5. .forEach()

// Syntax: array.forEach(function)

   let names = ["iptu", "Emon", "Ipat"];
   names.forEach(name => {
    console.log("Hello", + name); // Output:Hello NaN
                                           //Hello NaN
                                             //Hello NaN
   })